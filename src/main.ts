let mesas : number = 30
console.log(mesas);

let usuario : string = 'bruna'
console.log(usuario);

let temAula : boolean = true
console.log(temAula);

let brincadeira: undefined;
console.log(brincadeira);

interface objeto {
    marca: string;
    quatidade: number;
    eficiencia: boolean;
    idade: undefined;
}

let mouse: objeto = {
    marca: 'dell',
    quatidade: 30,
    eficiencia: true,
    idade: undefined
};

console.log(mouse);